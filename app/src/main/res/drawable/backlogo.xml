<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/logo" />
    <stroke
        android:width="1dp"
        android:color="#665AF0"
    android:dashWidth="4dp"
    android:dashGap="2dp" />

    <corners android:radius="10px" /> <!-- 设置四角弧度为 10dp -->
</shape>